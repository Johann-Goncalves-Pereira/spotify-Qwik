import { component$ } from "@builder.io/qwik";

export const SeparatorHorizontal = component$(() => {
  return <div></div>;
});

export const SeparatorVertical = component$(() => {
  return <div></div>;
});
